#ifndef LCDEFINE_H
#define LCDEFINE_H

#include "xtp_api_data_type.h"
#include "xtp_api_struct.h"

typedef enum XTP_ORDER_SUBMIT_STATUS_TYPE XTP_ORDER_SUBMIT_STATUS_TYPE_;

typedef enum XTP_EXCHANGE_TYPE XTP_EXCHANGE_TYPE_;

typedef enum XTP_FUND_OPER_STATUS XTP_FUND_OPER_STATUS_;

typedef enum XTP_LOG_LEVEL XTP_LOG_LEVEL_;

typedef enum XTP_MARKET_TYPE XTP_MARKET_TYPE_;

typedef enum XTP_TICKER_TYPE XTP_TICKER_TYPE_;

typedef enum XTP_BUSINESS_TYPE XTP_BUSINESS_TYPE_;

typedef enum XTP_ORDER_STATUS_TYPE XTP_ORDER_STATUS_TYPE_;

typedef enum XTP_TE_RESUME_TYPE XTP_TE_RESUME_TYPE_;

typedef enum XTP_ORDER_ACTION_STATUS_TYPE XTP_ORDER_ACTION_STATUS_TYPE_;

typedef enum XTP_PRICE_TYPE XTP_PRICE_TYPE_;

typedef enum XTP_SIDE_TYPE XTP_SIDE_TYPE_;

typedef enum XTP_FUND_TRANSFER_TYPE XTP_FUND_TRANSFER_TYPE_;

typedef enum XTP_PROTOCOL_TYPE XTP_PROTOCOL_TYPE_;
      
#ifdef __cplusplus
extern "C" {
#endif


#ifdef __cplusplus
}
#endif

#endif
